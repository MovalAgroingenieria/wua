<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <!-- Droplist action (wua.quota) -->
    <act_window id="action_nrs_wizard_quota"
        context="{'mode': 'quota'}"
        name="Send SMS (NRS)"
        res_model="nrs.wizard"
        src_model="wua.quota"
        key2="client_action_multi"
        view_mode="form"
        target="new"
        groups="nrs_client_sms.group_send_nrs_sms"/>

</odoo>