<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Inhibit Contacts app for non Wua users -->
    <record id="contacts.menu_contacts" model="ir.ui.menu">
        <field name="groups_id"
               eval="[(3, ref('base.group_user')),
                      (4, ref('base_wua.group_wua_user'))]"/>
    </record>

</odoo>