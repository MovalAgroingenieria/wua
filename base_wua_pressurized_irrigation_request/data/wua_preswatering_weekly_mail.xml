<odoo>
    <data noupdate="1">
        <record id="wua_preswatering_weekly_email_template" model="mail.template">
            <field name="name">Preswatering Weekly Report</field>
            <field name="model_id" ref="base.model_res_partner"/>
            <field name="subject">Weekly Watering Schedule</field>
            <field name="email_from">${(user.email or 'no-reply@yourdomain.com')|safe}</field>
            <field name="email_to">${object.email|safe}</field>
            <field name="body_html">
                <![CDATA[
                    <p>Dear <strong>${object.name}</strong>,</p>
                    <p>We are pleased to provide your watering schedule for the upcoming week. Below are the details for your scheduled consumptions:</p>

                    <table border="1" style="width: 100%; border-collapse: collapse; text-align: left;">
                        <thead>
                            <tr style="background-color: #f2f2f2;">
                                <th style="padding: 8px;">Date</th>
                                <th style="padding: 8px;">Water Connection</th>
                                <th style="padding: 8px;">Nominal Flow (l/s)</th>
                                <th style="padding: 8px;">Duration (h)</th>
                                <th style="padding: 8px;">Volume (m³)</th>
                            </tr>
                        </thead>
                        <tbody>
                            % for consumption in ctx['data']['consumptions']:
                                <tr>
                                    <td style="padding: 8px;">${consumption.get('date', '-')}</td>
                                    <td style="padding: 8px;">${consumption.get('water_connection', '-')}</td>
                                    <td style="padding: 8px;">${consumption.get('nominal_flow', '0.0')}</td>
                                    <td style="padding: 8px;">${consumption.get('watering_duration', '0')}</td>
                                    <td style="padding: 8px;">${consumption.get('watering_volume', '0.0')}</td>
                                </tr>
                            % endfor
                        </tbody>
                    </table>

                    <p>If you have any questions or need assistance, please do not hesitate to contact us.</p>
                    <p>Best regards,<br>Your Water Authority Team</p>
                ]]>
            </field>
        </record>
    </data>
</odoo>
