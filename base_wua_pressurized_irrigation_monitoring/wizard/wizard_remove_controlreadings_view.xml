<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="wizard_remove_controlreadings_view_form" model="ir.ui.view">
        <field name="name">Wizard to delete control readings</field>
        <field name="model">wizard.remove.controlreadings</field>
        <field name="arch" type="xml">
            <form string="Delete control readings">
                <script>
                    $(document).ready(function(){
                        $('.modal-dialog').css({'width': '450px'});
                        $('.modal-dialog').css({'height': '420px'});
                    });
                </script>
                <group name="body">
                    <field name="initial_date"/>
                    <field name="end_date"/>
                    <field name="num_controlreadings_to_delete"/>
                </group>
                <div>
                    NOTE: The date range must include the last control reading.
                </div>
                <footer>
                    <button name="get_delete_period"
                            type="object"
                            string="Delete"
                            class="btn-primary"/>
                    <button string="Cancel_"
                            class="btn-default"
                            special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <act_window id="action_delete_controlreadings"
        name="Delete control readings"
        res_model="wizard.remove.controlreadings"
        src_model="wua.controlreading"
        view_mode="form"
        target="new"
        groups="base_wua.group_wua_manager"/>

</odoo>